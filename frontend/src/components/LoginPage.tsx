import LoginForm from './LoginForm';
import RegisterButton from './RegisterButton';

const LoginPage = () => {
  return (
    <div>
      <LoginForm />
      <RegisterButton />
    </div>
  );
};

export default LoginPage;
